<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="#">Navbar</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav">
      <%= link_to "Home", messages_path, class: 'nav-item nav-link active' %>
      <%= link_to "Keys", public_keys_path, class: 'nav-item nav-link' %>

      </div>
      </div>
      <% if user_signed_in? %>
        <span class="nav-item nav-link text-white"><%= current_user.username %></span>
        <%= link_to "Sair", destroy_user_session_path, class: 'btn btn-outline-success my-2 my-sm-0', method: :delete %>
      <% else %>
        <%= link_to "Entrar", new_user_session_path, class: 'btn btn-outline-success my-2 my-sm-0' %>
      <% end %>
      
</div>
</nav>