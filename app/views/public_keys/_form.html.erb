<%= form_with(model: public_key, local: true) do |form| %>
  <% if public_key.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(public_key.errors.count, "error") %> prohibited this public_key from being saved:</h2>

      <ul>
        <% public_key.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :key %>
    <%= form.text_field :key %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
